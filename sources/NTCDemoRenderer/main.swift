// NTCDemoRenderer — plane viewer that calls into ntc_decode.
// Phase A scaffolding only (real entry point becomes App.swift + Renderer.swift later).
import NTCInference
import NTCAssets

print("NTCDemoRenderer scaffold — NTCInference core=\(NTCInference.coreVersion), NTCAssets core=\(NTCAssets.coreVersion)")
